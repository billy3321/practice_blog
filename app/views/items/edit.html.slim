h1 Editing item

= simple_form_for(@item, html: {class: 'form-horizontal center' }) do |f|
  = render :partial => 'form', :locals => { :f => f }

= link_to 'Show', item_path(@item), class: "btn"
= link_to 'Back', items_path, class: "btn"
