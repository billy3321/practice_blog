h1 Editing photo

= simple_form_for(@photo, html: {class: 'form-horizontal center' }) do |f|
  = render :partial => 'form', :locals => { :f => f }

= link_to 'Show', photo_path(@photo), class: "btn"
= link_to 'Back', photos_path, class: "btn"
